# Environment variables example
# Copy this file to .env and fill in the values

# NODE_ENV=development

GITHUB_TOKEN=pat_for_mcp_server

# AI summarization uses `claude -p` (Claude Code CLI). No API key needed.
# Disable with NOTIFY_SUMMARIZE=false if claude CLI is not available.
# NOTIFY_SUMMARIZE=false